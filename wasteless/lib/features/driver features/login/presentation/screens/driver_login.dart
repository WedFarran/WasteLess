import 'package:flutter/material.dart';

class DriverLogIn extends StatelessWidget {
  static const String id = 'DriverLogIn';
  const DriverLogIn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
