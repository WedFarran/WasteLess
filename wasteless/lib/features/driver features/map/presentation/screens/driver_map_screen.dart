import 'package:flutter/material.dart';

class DriverDriverScreen extends StatelessWidget {
  static const String id = 'driver_map_screen';
  const DriverDriverScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(child: Text('map screen'));
  }
}
